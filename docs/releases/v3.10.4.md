# v3.10.4 — leave posted Odoo lines alone; explain live-balance gaps honestly

- **Push skips posted Stripe lines.** Refreshing a label/narration on an
  existing line meant draft → write → repost; Odoo refuses that on posted
  entries ("You can't delete a posted journal item"), producing one warning
  per line (56 on the first prod run). Posted lines are now left untouched
  and reported once. Same for partner linking in `reconcile`.
- **Balance warning tells two stories apart.** "Odoo ≠ live" previously
  lumped together *the local mirror is behind the source* (new activity since
  the last pull — the next run catches up; Odoo matches the mirror) and *the
  journal drifted from the mirror* (needs `fix`). The first prod push showed
  a −128.60 EUR "drift" on Stripe that was two charges received after the
  pull. The message now names which case it is and hints accordingly.
