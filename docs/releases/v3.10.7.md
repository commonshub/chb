# v3.10.7 — only the URL field makes a calendar entry a public event

Room bookings whose description mentioned a website, a maps pin or a mail
thread were listed on the website as events. Only an entry's own `URL:`
property counts now; links in the description or location are ignored.
Set the URL field, or add the event to the Commons Hub Luma calendar, to
have a booking listed.
