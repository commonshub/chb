# v3.10.8 — public calendars keep their links

v3.10.7 required the ICS `URL:` property for an event to be public, but
Luma's feed has no `URL:` property; its link is in the description. A
public calendar (Luma) now takes the link from the description, while a
room calendar still needs the URL field for an entry to count as an event.
